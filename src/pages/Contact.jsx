import { ContactForm } from "5irad-components";

const Contact = () => {
    return (
        <section>
            <h1>Contact us (in progress)</h1>
            <ContactForm />
        </section>
    );
};

export default Contact;